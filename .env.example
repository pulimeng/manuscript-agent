# Copy to .env (next to pyproject.toml, or in the directory you run from) and fill in.
# .env is gitignored. Values already set in your shell take precedence over this file.
ANTHROPIC_API_KEY=sk-ant-...

# Optional. Without it, the panel is drawn from the Claude models in MODEL_POOL only.
OPENAI_API_KEY=sk-...
